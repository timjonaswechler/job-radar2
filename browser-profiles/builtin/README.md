# Obsolete built-in browser-profile seed inputs

These JSON files are kept only as temporary compatibility input for the pre-#38 SQLite seed path. Authoritative built-in source-profile documents now live in `../../source-profiles/builtin/`, and built-in sources live in `../../sources/builtin/`.

Do not add new built-in source knowledge here.
