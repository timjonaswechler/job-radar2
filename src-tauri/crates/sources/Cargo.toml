[package]
name = "sources"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
source-profile-dsl = { path = "../source-profile-dsl" }

[dev-dependencies]
tempfile = "3"
